
  # Gamification Strategy Dashboard UI

  This is a code bundle for Gamification Strategy Dashboard UI. The original project is available at https://www.figma.com/design/Zirr8eTouL8tfNtUZOi8RL/Gamification-Strategy-Dashboard-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  